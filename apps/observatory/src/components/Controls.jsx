import React from 'react';
export function Slider({label,id,value,min,max,step,onChange,hint}){
 return <div className="slider-field"><label htmlFor={id}><span>{label}</span><output htmlFor={id}>{value.toFixed(2)}</output></label><input id={id} type="range" min={min} max={max} step={step} value={value} onChange={e=>onChange(Number(e.target.value))}/><div className="range-ticks"><span>{min.toFixed(2)}</span><span>{((min+max)/2).toFixed(2)}</span><span>{max.toFixed(2)}</span></div>{hint&&<p className="hint">{hint}</p>}</div>;
}
export function ParameterRail({params,update,reset}){return <aside className="parameter-rail"><h2>Synthetic counterexample</h2><Slider id="substrate" label="Latent functional substrate" value={params.substrate} min={.2} max={1} step={.01} onChange={v=>update('substrate',v)}/><Slider id="gainA" label="Response efficacy A" value={params.gainA} min={.2} max={2} step={.01} onChange={v=>update('gainA',v)}/><Slider id="gainB" label="Response efficacy B" value={params.gainB} min={.2} max={2} step={.01} onChange={v=>update('gainB',v)}/><Slider id="threshold" label="Function threshold" value={params.threshold} min={.2} max={1.2} step={.01} onChange={v=>update('threshold',v)}/><button className="outline small" onClick={reset}>Reset</button><p className="hint">Hidden model variables. MRI does not measure substrate or response efficacy.</p></aside>}
export function ResultBand({title,children,button,onClick}){return <section className="result-band"><div><h2>{title}</h2><p>{children}</p></div>{button&&<button className="primary" onClick={onClick}>{button}</button>}</section>}
export function Legend(){return <div className="legend"><span><i/> Model A</span><span><i className="coral"/> Model B</span></div>}
