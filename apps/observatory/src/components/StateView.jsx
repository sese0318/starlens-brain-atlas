import React from 'react';
import {RegionalChart,FunctionBars} from './Charts.jsx';
import {ParameterRail,ResultBand,Legend} from './Controls.jsx';
export default function StateView({params,update,reset,result,profile,bundle,onNext}){
 return <><div className="workspace state-workspace"><ParameterRail params={params} update={update} reset={reset}/><section className="plot-panel"><h2>Identical structural observations</h2><Legend/><RegionalChart values={profile.values}/><p className="plot-caption">Identical MRI input and identical molecular associations.</p><p className="data-note">{bundle.kind==='synthetic'?'Synthetic profile and 30 synthetic references.':`${profile.label} · existing research aggregate, reproduction pending.`}</p></section><aside className="states-rail"><h2>Hidden functional states</h2>{[result.a,result.b].map((s,i)=><div className={`state-value ${i?'state-b':'state-a'}`} key={i}><h3>Model {i?'B':'A'} function</h3><strong data-testid={i?'function-b':'function-a'}>{s.output.toFixed(2)}</strong><div className="state-label">{s.label}</div></div>)}<FunctionBars a={result.a} b={result.b} threshold={params.threshold}/><p className="hint">F = substrate × efficacy. Labels use an assumed threshold and do not classify biological cells.</p></aside></div><ResultBand title={result.functionGap<1e-8?'The two functions now coincide.':'These observations do not identify function.'} button="Choose the next measurement" onClick={onNext}>{result.functionGap<1e-8?'Equal gains remove this counterexample. Change a gain to construct different outputs.':'Within this observation model, a functional measurement can separate the two explanations.'}</ResultBand></>;
}
